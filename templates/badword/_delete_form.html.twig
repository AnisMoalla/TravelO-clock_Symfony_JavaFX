<form method="post" action="{{ path('badword_delete', {'id': badword.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ badword.id) }}">
    <button class="btn">Delete</button>
</form>
