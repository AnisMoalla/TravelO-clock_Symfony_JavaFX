<!DOCTYPE html>

<title>Edit Badword</title>

{% block body %}
    <h1>Edit Badword</h1>

    {{ include('badword/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('badword_index') }}">back to list</a>

    {{ include('badword/_delete_form.html.twig') }}
{% endblock %}
