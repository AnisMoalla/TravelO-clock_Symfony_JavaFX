<form method="post" action="{{ path('facceuil_delete', {'id': facceuil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facceuil.id) }}">
    <button class="btn">Delete</button>
</form>
