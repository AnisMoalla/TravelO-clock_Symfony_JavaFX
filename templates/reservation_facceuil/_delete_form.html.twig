<form method="post" action="{{ path('reservation_facceuil_delete', {'id': reservation_facceuil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_facceuil.id) }}">
    <button class="btn">Delete</button>
</form>
