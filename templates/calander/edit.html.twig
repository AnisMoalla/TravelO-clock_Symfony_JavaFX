<!DOCTYPE html>

<title>Edit Calander</title>

{% block body %}
    <h1>Edit Calander</h1>

    {{ include('calander/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('calander_index') }}">back to list</a>

    {{ include('calander/_delete_form.html.twig') }}
{% endblock %}
