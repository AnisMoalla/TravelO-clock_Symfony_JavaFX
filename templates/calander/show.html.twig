<!DOCTYPE html>

<title>Calander</title>

{% block body %}
    <h1>Calander</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calander.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ calander.title }}</td>
            </tr>
            <tr>
                <th>Date_begin</th>
                <td>{{ calander.dateBegin ? calander.dateBegin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Data_fin</th>
                <td>{{ calander.dataFin ? calander.dataFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('calander_index') }}">back to list</a>

    <a href="{{ path('calander_edit', {'id': calander.id}) }}">edit</a>

    {{ include('calander/_delete_form.html.twig') }}
{% endblock %}
