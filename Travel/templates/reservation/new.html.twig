
{% extends 'baseBack.html.twig' %}

{% block title %}New Reservation{% endblock %}

{% block content %}
    <h1>Create new Reservation</h1>

    <div>{{ include('reservation/_form.html.twig') }}</div>

    <a href="{{ path('reservations') }}">back to list</a>
{% endblock %}
