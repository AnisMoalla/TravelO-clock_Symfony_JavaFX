{% extends 'base.html.twig' %}

{% block title %}Facceuil{% endblock %}

{% block body %}
    <h1>Facceuil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facceuil.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ facceuil.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ facceuil.adresse }}</td>
            </tr>
            <tr>
                <th>Nb_place</th>
                <td>{{ facceuil.nbPlace }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ facceuil.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ facceuil.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('facceuil_index') }}">back to list</a>

    <a href="{{ path('facceuil_edit', {'id': facceuil.id}) }}">edit</a>

    {{ include('facceuil/_delete_form.html.twig') }}
{% endblock %}

