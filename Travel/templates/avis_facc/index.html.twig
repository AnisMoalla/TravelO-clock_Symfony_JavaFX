<!DOCTYPE html>

<title>AvisFacc index</title>

{% block body %}
    <h1>AvisFacc index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Rating</th>
                <th>Description</th>
                <th>Date_publication</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avis_facc in avis_faccs %}
            <tr>
                <td>{{ avis_facc.id }}</td>
                <td>{{ avis_facc.rating }}</td>
                <td>{{ avis_facc.description }}</td>
                <td>{{ avis_facc.datePublication ? avis_facc.datePublication|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('avis_facc_show', {'id': avis_facc.id}) }}">show</a>
                    <a href="{{ path('avis_facc_edit', {'id': avis_facc.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('avis_facc_new') }}">Create new</a>
{% endblock %}
