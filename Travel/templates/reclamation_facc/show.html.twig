<!DOCTYPE html>

<title>ReclamationFacc</title>

{% block body %}
    <h1>ReclamationFacc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation_facc.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation_facc.description }}</td>
            </tr>
            <tr>
                <th>Date_reclamation</th>
                <td>{{ reclamation_facc.dateReclamation ? reclamation_facc.dateReclamation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_facc_index') }}">back to list</a>

    <a href="{{ path('reclamation_facc_edit', {'id': reclamation_facc.id}) }}">edit</a>

    {{ include('reclamation_facc/_delete_form.html.twig') }}
{% endblock %}
