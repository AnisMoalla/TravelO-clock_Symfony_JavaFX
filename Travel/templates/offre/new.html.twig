<!DOCTYPE html>

<title>New Offre</title>

{% block body %}
    <h1>Create new Offre</h1>

    {{ include('offre/_form.html.twig') }}

    <a href="{{ path('offre_index') }}">back to list</a>
{% endblock %}
