{% extends 'baseBack.html.twig' %}

{% block title %}PostForum{% endblock %}

{% block content %}
    <h1>Post </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_forum.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post_forum.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post_forum.content }}</td>
            </tr>
            <tr>
                <th>ser</th>
                <td>{{ post_forum.user}}</td>
            </tr>
            <tr>
                <th>categorie</th>
                <td>{{ post_forum.category}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_forum_index') }}">back to list</a>

    <a href="{{ path('post_forum_edit', {'id': post_forum.id}) }}">edit</a>

    {{ include('post_forum/_delete_form.html.twig') }}
{% endblock %}
