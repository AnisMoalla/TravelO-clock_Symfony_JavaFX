<form method="post" action="{{ path('post_forum_delete', {'id': post_forum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_forum.id) }}">
    <button class="btn">Delete</button>
</form>
